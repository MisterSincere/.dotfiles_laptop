# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Ubuntu Regular 8
#font pango:DejaVu Sans Mono 8

# xss lock to grab suspend event and add custom cmds
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# networkmanager applet
exec --no-startup-id nm-applet

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+q kill

# start rofi (a program launcher)
bindsym $mod+d exec $HOME/.config/rofi/scripts/start_launcher.sh

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+w layout toggle tabbed split
bindsym $mod+Shift+w layout toggle split splitv

bindsym $mod+e exec --no-startup-id dolphin

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+o focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1  "1:  "
set $ws2  "2:  "
set $ws3  "3:  "
set $ws4  "4:  "
set $ws5  "5:  "
set $ws6  "6:  "
set $ws7  "7:  "
set $ws8  "8:"
set $ws9  "9:"
set $ws10 "10:"

# Assign programs to workspaces
# use xprop
#for_window [class="firefox"]                    move to workspace $ws2
for_window [class="jetbrains-clion"]            move to workspace $ws3
for_window [class="Raft"]                       move to workspace $ws3
for_window [class="VirtualBox Manager"]         move to workspace $ws3
for_window [class="Spotify"]                    move to workspace $ws4
for_window [class="discord"]                    move to workspace $ws5
for_window [class="Slack"]                      move to workspace $ws5
for_window [class="Signal"]                     move to workspace $ws5
for_window [class="whatsapp-nativefier-d40211"] move to workspace $ws5
for_window [class="Skype"]                      move to workspace $ws5
for_window [class="Telegram"]                   move to workspace $ws5
for_window [class="Steam"]                      move to workspace $ws6
for_window [class="Lutris"]                     move to workspace $ws6
for_window [class="Thunderbird"]                move to workspace $ws7

# Assign workspaces to screens
workspace $ws1 output DP-2
workspace $ws2 output DP-2
workspace $ws3 output DP-2
workspace $ws4 output HDMI-0
workspace $ws5 output HDMI-0
workspace $ws6 output HDMI-0
workspace $ws7 output HDMI-0

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# move focused workspace to screen
bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+Left move workspace to output left
bindsym $mod+Ctrl+l move workspace to output right
bindsym $mod+Ctrl+Right move workspace to output right

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

bindsym Mod1+Tab workspace next
bindsym Mod1+Shift+Tab workspace prev
bindsym $mod+Tab exec rofi -show window

# projection / monitor selection
bindsym $mod+Shift+p exec ~/.config/rofi/scripts/project_to.sh

# audio output selection
bindsym $mod+a exec ~/.config/rofi/scripts/start_sel_audio_out.sh
#bindsym $mod+a exec rofi -show Output -modi "Output:~/.config/rofi/scripts/sel_audio_out.sh" -i -font "Ubuntu Light 14" -hide-scrollbar

# most used screenshot for me
bindsym $mod+s exec maim -s -u | xclip -selection clipboard -t image/png -i
bindsym $mod+Shift+s exec exec ~/.config/rofi/scripts/start_screen_cap.sh

bindsym $mod+x exec colorpicker --one-shot --preview --short | xclip -selection clipboard

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode
  bindsym h resize shrink width 10 px or 10 ppt
  bindsym k resize grow height 10 px or 10 ppt
  bindsym j resize shrink height 10 px or 10 ppt
  bindsym l resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# workspace binding program launch
mode "launch" {
  bindsym f exec firefox, mode "default"
  bindsym t exec thunderbird, mode "default"
  bindsym Shift+t exec telegram-desktop, mode "default"
  bindsym s exec spotify, mode "default"
  bindsym Shift+s exec steam, mode "default"
  bindsym Ctrl+s exec slack, mode "default"
  bindsym d exec discord, mode "default"
  bindsym w exec whatsapp-nativefier, mode "default"
  bindsym c exec clion, mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+m mode "default"
}
bindsym $mod+m mode "launch"

set $locker ~/.config/i3/scripts/lock.sh
mode "system" {
  bindsym e exec i3-msg exit, mode "default"
  bindsym l exec $locker, mode "default"
  bindsym p exec --no-startup-id systemctl poweroff -i, mode "default"
  bindsym r exec --no-startup-id systemctl reboot, mode "default"
  bindsym s exec --no-startup-id $locker && systemctl suspend, mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+c mode "default"
}
bindsym $mod+c mode "system"

# Window Colors
set $dgrey      #363636CC
set $grey       #565656FF
set $lgrey      #767676FF
set $llgrey     #969696FF
set $dorange    #753A27FF
set $orange     #FF6A00FF
set $white      #ffffffFF
set $black      #000000FF
set $red        #e53935FF
set $best       #df20df88

# 			                border		background	text		indicator
client.focused		      $best     $best       $white  $red
client.unfocused	      $dgrey    $dgrey      $white  $dgrey
client.focused_inactive $dgrey    $dgrey      $white  $dgrey
client.urgent		        $red      $red        $white  $red

# Gaps
for_window [class=".*"] border pixel 2
gaps inner 0
gaps outer 0

# Origin thinks it's special
for_window [instance="origin.exe"] floating enable
for_window [instance="Raft"] floating enable

# Keyboard Layout
#exec_always --no-startup-id setxkbmap de

# Display Wallpaper
exec_always --no-startup-id feh --bg-fill $HOME/Pictures/wp3.jpg

# Polybar Launch Script
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# dunst
exec_always --no-startup-id /usr/bin/dunst

exec_always --no-startup-id $HOME/.config/polybar/scripts/checkupdates.sh
